# Test Specifications — Tabular
## Unit: memory
## Initiative: Multi-Tenancy Model Implementation
## Scope: happy-path-negative

| Test Case ID | Title | Preconditions | Steps | Expected Result | Priority | Source | Construction Path |
|---|---|---|---|---|---|---|---|
| TC-M-001 | Alembic migration drops tenants and users tables | PostgreSQL test DB; pre-migration schema | 1. Apply Alembic migration; 2. Check table list | tenants and users tables absent; no FK constraints reference them | High | memory / FR-3.1, FR-3.2 | aidlc-docs/platform/multi-tenancy/construction/memory/ |
| TC-M-002 | All 8 tables have three-column identity after migration | Alembic migration applied | 1. Inspect each of 8 tables for column types | Each table has platform_tenant_id, service_tenant_id, service_user_id as VARCHAR(64); no UUID/FK types | High | memory / FR-3.3, FR-3.5 | aidlc-docs/platform/multi-tenancy/construction/memory/ |
| TC-M-003 | RLS policies enforce platform_tenant_id isolation | Migrated DB; rows under two different platform_tenant_ids | 1. Set session variable to spt_tenant_1; 2. Query semantic_memory | Only spt_tenant_1 row returned; spt_tenant_2 row invisible | High | memory / FR-3b.2, FR-3b.4 | aidlc-docs/platform/multi-tenancy/construction/memory/ |
| TC-M-004 | Memory API stores and retrieves with three-column identity | Memory Service running; migrated schema; TenancyMiddleware registered | 1. POST /semantic-memory with 3 headers; 2. GET /semantic-memory with same headers | POST returns 201; GET returns 200 with stored item | High | memory / FR-3.8, FR-3.9 | aidlc-docs/platform/multi-tenancy/construction/memory/ |
| TC-M-005 | delete_by_platform_tenant removes all rows for that tenant | Rows in 3 tables under spt_delete_me | 1. Call delete_by_platform_tenant("spt_delete_me"); 2. Query all 3 tables | Zero rows for spt_delete_me in all tables; other tenants unaffected | High | memory / FR-4.1, FR-4.2 | aidlc-docs/platform/multi-tenancy/construction/memory/ |
| TC-M-006 | delete_by_service_tenant scoped within platform tenant | Rows (spt_1, st1) and (spt_2, st1) in semantic_memory | 1. Call delete_by_service_tenant("spt_1", "st1"); 2. Query both rows | (spt_1, st1) deleted; (spt_2, st1) remains | High | memory / FR-4.1, NFR-1.3 | aidlc-docs/platform/multi-tenancy/construction/memory/ |
| TC-M-007 | Memory Service uses shared TenancyMiddleware | Memory Service codebase | 1. Inspect main.py and absence of local TenancyMiddleware | TenancyMiddleware imported from soorma_service_common; no local implementation | High | memory / FR-3.6 | aidlc-docs/platform/multi-tenancy/construction/memory/ |
| TC-M-008 | Rebuilt RLS policies use string comparison | Alembic migration applied | 1. Query pg_policies for memory table policies; 2. Inspect qual expressions | No ::uuid cast in any policy; string equality on current_setting values | High | memory / FR-3b.1, FR-3b.2 | aidlc-docs/platform/multi-tenancy/construction/memory/ |
| TC-M-009 | Query without RLS session variables returns 0 rows | Rows in semantic_memory under known tenant | 1. Execute raw query on semantic_memory without calling set_config | Zero rows returned | High | memory / FR-3b.2, FR-3b.4 | aidlc-docs/platform/multi-tenancy/construction/memory/ |
| TC-M-010 | Memory API rejects missing service tenant for user-scoped ops | Memory Service running | 1. POST /semantic-memory with X-Tenant-ID and X-User-ID, no X-Service-Tenant-ID | HTTP 400 or 422; error message references service_tenant_id | High | memory / FR-3.8 | aidlc-docs/platform/multi-tenancy/construction/memory/ |
| TC-M-011 | delete_by_service_user only deletes the specific user | Rows (spt_1, st1, user_A) and (spt_1, st1, user_B) in working_memory | 1. Call delete_by_service_user("spt_1", "st1", "user_A"); 2. Query both rows | user_A row deleted; user_B row remains | High | memory / FR-4.1, NFR-1.3 | aidlc-docs/platform/multi-tenancy/construction/memory/ |
