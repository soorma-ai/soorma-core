# Test Specifications — Tabular
## Unit: memory
## Initiative: Multi-Tenancy Model Implementation
## Scope: happy-path-negative

| Test Case ID | Title | Preconditions | Steps | Expected Result | Priority | Source | Construction Path |
|---|---|---|---|---|---|---|---|
| TC-M-001 | Alembic migration drops tenants and users tables | PostgreSQL test DB; pre-migration schema with `tenants` and `users` tables | 1. Apply migration `008_multi_tenancy_three_column_identity.py`; 2. Query `information_schema.tables` for `tenants`/`users`; 3. Check `pg_constraint` for FKs to `tenants.id`/`users.id` | `information_schema.tables` returns 0 rows for both; no FK constraints reference either table; drop order: RLS policies → FK constraints → tables (BR-U4-04) | High | memory / FR-3.1, FR-3.2 | aidlc-docs/platform/multi-tenancy/construction/memory/ |
| TC-M-002 | All 8 tables have three-column identity after migration | Migration `008_multi_tenancy_three_column_identity.py` applied | 1. Query `information_schema.columns` for each of 8 tables (`semantic_memory`, `episodic_memory`, `procedural_memory`, `working_memory`, `task_context`, `plan_context`, `plans`, `sessions`); 2. Inspect column types and nullability | Each table has `platform_tenant_id VARCHAR(64) NOT NULL`, `service_tenant_id VARCHAR(64)` nullable, `service_user_id VARCHAR(64)` nullable; no UUID-typed tenant/user FK columns; ORM: `services/memory/src/memory_service/models/memory.py` | High | memory / FR-3.3, FR-3.5 | aidlc-docs/platform/multi-tenancy/construction/memory/ |
| TC-M-003 | RLS policies enforce platform_tenant_id isolation | Migrated DB; rows under `spt_tenant_1` and `spt_tenant_2` in `semantic_memory` | 1. Call `get_tenanted_db` (`services/memory/src/memory_service/core/dependencies.py`) which sets `set_config('app.platform_tenant_id', 'spt_tenant_1', true)` (transaction-scoped); 2. Query `semantic_memory` | Only `spt_tenant_1` row returned; `spt_tenant_2` row invisible; policy `semantic_memory_platform_rls` uses string equality `USING (platform_tenant_id = current_setting('app.platform_tenant_id', true))` | High | memory / FR-3b.2, FR-3b.4 | aidlc-docs/platform/multi-tenancy/construction/memory/ |
| TC-M-004 | Memory API stores and retrieves with three-column identity | Memory Service running; migrated schema; `TenancyMiddleware` from `soorma_service_common` registered in `main.py` | 1. POST `/semantic-memory` with `X-Tenant-ID: spt_abc`, `X-Service-Tenant-ID: tenant_xyz`, `X-User-ID: user_123`; 2. GET `/semantic-memory` with same 3 headers | POST returns 201; GET returns 200 with stored item; response body includes `platform_tenant_id`, `service_tenant_id`, `service_user_id` (BR-U4-11); legacy `tenant_id` absent; router: `api/v1/semantic.py` | High | memory / FR-3.8, FR-3.9 | aidlc-docs/platform/multi-tenancy/construction/memory/ |
| TC-M-005 | delete_by_platform_tenant removes all rows across 6 covered tables | Rows in `semantic_memory`, `episodic_memory`, `task_context` under `spt_delete_me` | 1. Call `MemoryDataDeletion.delete_by_platform_tenant(db, "spt_delete_me")` (`services/memory/src/memory_service/services/data_deletion.py`); 2. Query all 3 tables | Zero rows for `spt_delete_me` in all 3 tables; other tenants unaffected; `MemoryDataDeletion` extends `PlatformTenantDataDeletion` from `soorma_service_common`; covers exactly 6 tables (BR-U4-06) | High | memory / FR-4.1, FR-4.2 | aidlc-docs/platform/multi-tenancy/construction/memory/ |
| TC-M-006 | delete_by_service_tenant scoped within platform tenant | Rows `(spt_1, st1, u1)` and `(spt_2, st1, u1)` in `semantic_memory` | 1. Call `MemoryDataDeletion.delete_by_service_tenant(db, platform_tenant_id="spt_1", service_tenant_id="st1")`; 2. Query both rows | `(spt_1, st1, u1)` deleted; `(spt_2, st1, u1)` remains; WHERE pattern (Pattern 3): `platform_tenant_id == p AND service_tenant_id == st` | High | memory / FR-4.1, NFR-1.3 | aidlc-docs/platform/multi-tenancy/construction/memory/ |
| TC-M-007 | Memory Service uses shared TenancyMiddleware | Memory Service codebase after migration | 1. Check `services/memory/src/memory_service/core/middleware.py` exists (must NOT); 2. Inspect `main.py` for import and registration | File `core/middleware.py` is deleted entirely (BR-U4-09); `main.py` imports `TenancyMiddleware` from `soorma_service_common` and calls `app.add_middleware(TenancyMiddleware)` | High | memory / FR-3.6 | aidlc-docs/platform/multi-tenancy/construction/memory/ |
| TC-M-008 | Rebuilt RLS policies use string comparison and FORCE RLS | Migration `008_multi_tenancy_three_column_identity.py` applied | 1. Query `pg_policies WHERE tablename IN (8 memory tables)`; 2. Inspect `qual` expressions and `pg_class.relrowsecurity`/`relforcerowsecurity` | 8 policies named `{table_name}_platform_rls`; each `qual`: `platform_tenant_id = current_setting('app.platform_tenant_id', true)`; no `::uuid` cast; `rowsecurity=true`, `forcersls=true` for all 8 tables (Pattern 1, step 13+14) | High | memory / FR-3b.1, FR-3b.2 | aidlc-docs/platform/multi-tenancy/construction/memory/ |
| TC-M-009 | Query without RLS session variables returns 0 rows | Row in `semantic_memory` under known `platform_tenant_id` | 1. Obtain raw `AsyncSession` from `create_async_engine(settings.database_url)` bypassing `get_tenanted_db`; 2. Query `SELECT * FROM semantic_memory` (no `set_config` called) | Zero rows returned; `current_setting('app.platform_tenant_id', true)` returns `''` (`missing_ok=true`); `''` matches no stored `platform_tenant_id`; no PostgreSQL error | High | memory / FR-3b.2, FR-3b.4 | aidlc-docs/platform/multi-tenancy/construction/memory/ |
| TC-M-010 | Memory API rejects missing service tenant for user-scoped ops | Memory Service running with `TenancyMiddleware` from `soorma_service_common` | 1. POST `/semantic-memory` with `X-Tenant-ID: spt_abc`, `X-User-ID: user_123`; omit `X-Service-Tenant-ID` | HTTP 422 or 400; error references `service_tenant_id` as required; `TenancyMiddleware` sets `service_tenant_id=""` when header absent; BR-U4-02: `service_user_id` operations require non-empty `service_tenant_id` | High | memory / FR-3.8 | aidlc-docs/platform/multi-tenancy/construction/memory/ |
| TC-M-011 | delete_by_service_user only deletes the specific user | Rows `(spt_1, st1, user_A)` and `(spt_1, st1, user_B)` in `working_memory` | 1. Call `MemoryDataDeletion.delete_by_service_user(db, "spt_1", "st1", "user_A")`; 2. Query both rows | `user_A` row deleted; `user_B` row remains; WHERE pattern (Pattern 3): all 3 columns enforced; signature: `(db, platform_tenant_id, service_tenant_id, service_user_id) -> int` | High | memory / FR-4.1, NFR-1.3 | aidlc-docs/platform/multi-tenancy/construction/memory/ |
| TC-M-012 | MemoryDataDeletion does not delete plans or sessions rows | Rows in `plans`, `sessions`, and `semantic_memory` all under `spt_delete_me` | 1. Call `MemoryDataDeletion.delete_by_platform_tenant(db, "spt_delete_me")`; 2. Query `semantic_memory`, `plans`, `sessions` | `semantic_memory` rows deleted; `plans` and `sessions` rows remain; `MemoryDataDeletion` covers exactly 6 tables — `Plan` and `Session` models are NOT referenced (BR-U4-06) | High | memory / BR-U4-06 | aidlc-docs/platform/multi-tenancy/construction/memory/ |
| TC-M-013 | Admin deletion endpoint activates RLS session before bulk delete | Rows in `semantic_memory` and `episodic_memory` under `spt_admin_delete` | 1. Call admin deletion endpoint for `spt_admin_delete`; 2. Check response; 3. Query both tables | HTTP 200 with rows-deleted count > 0; all rows for `spt_admin_delete` deleted; endpoint uses `Depends(get_db)` + `set_config_for_session(db, platform_tenant_id, "", "")` from `soorma_service_common` (not `get_tenanted_db`); router: `api/v1/admin.py` (BR-U4-08, Pattern 4) | High | memory / BR-U4-08, Pattern 4 | aidlc-docs/platform/multi-tenancy/construction/memory/ |
